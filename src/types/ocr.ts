// ===================================================================
// OCR 引擎类型定义 — 仅 Windows 系统 OCR
// ===================================================================

/** 支持的 OCR 引擎 */
export type OCREngine = "windows";

/** 所有引擎列表 */
export const OCR_ENGINES: OCREngine[] = ["windows"];

/** 默认引擎 */
export const DEFAULT_OCR_ENGINE: OCREngine = "windows";
